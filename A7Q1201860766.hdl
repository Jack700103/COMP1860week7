CHIP DecoderSegment201860766 {
    IN A, B, C, D;
    OUT c;

    PARTS:
    Not(in=A, out=notA);
    Not(in=D, out=notD);
    Not(in=C, out=notC);
    
    And(a=notD, b=notC, out=w1);
    And(a=w1, b=B, out=w2);
    And(a=w2, b=notA, out=term1);
    
    Or(a=B, b=notA, out=w3);
    And(a=D, b=C, out=w4);
    And(a=w4, b=w3, out=term2);
    
    Or(a=term1, b=term2, out=orOut);
    
    Not(in=orOut, out=c);
}